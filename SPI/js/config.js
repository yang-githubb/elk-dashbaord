/**
 * Dashboard settings — edit environment or overrides here.
 * Cluster URLs/index patterns: config/environments.js
 */
window.DASHBOARD_USER_CONFIG = {
  environment: "factory-sac",

  // overrides: {
  //   defaultTimeRange: "24h",
  //   pageSize: 50,
  // },
};
